import { defineConfig } from 'dz-leetcode'

export default defineConfig({
  // isTranslated: true, // true is 'zh_CN', false is 'en_US'
  lang: [
    'typescript',
    'javascript',
  ],
  onlyDoc: false,
  file: 'examples/structures.yml',
  // category: 'algorithms',
  // tag: 'array',
  // interactive: false,
})
